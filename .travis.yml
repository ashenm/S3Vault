sudo:
  required

language:
  minimal

install:
  - ./.travis/install.sh

before_script:
  - ./.travis/verify.sh

script:
  - ./.travis/build.sh

deploy:
  provider: script
  skip_cleanup: true
  script: ./.travis/deploy.sh
